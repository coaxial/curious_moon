---
version: '3'
services:
  database:
    image: postgres:10
    environment:
      PGPASSWORD: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./:/repo
volumes:
  pgdata:
